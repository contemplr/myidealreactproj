function Header () {
    return <header style={{
        position: 'fixed', top: 0, backgroundColor: 'white',
        width: '100vw'
    }}>
     Testing what might be a good idea for an header in contvarCompanyName!
    </header>
}

export default Header